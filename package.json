{
  "name": "shadowbase",
  "version": "0.1.0",
  "private": true,
  "description": "ShadowBase — Privacy-native forked Base (OP Stack) chain",
  "scripts": {
    "extract-vkeys": "node scripts/extract-vkeys.js",
    "generate-test-proof": "node scripts/generate-test-proof.js",
    "verify-sepolia": "node scripts/verify-sepolia-vkey.js",
    "phase1": "npm run extract-vkeys && npm run generate-test-proof && forge test -vvv --match-contract VerifierTest",
    "phase1:verify": "npm run phase1 && npm run verify-sepolia",
    "probe-railgun": "node scripts/probe-railgun-sepolia.js",
    "check-l2": "node scripts/check-l2-devnet.js",
    "verify-l1": "node scripts/verify-phase2-complete.js",
    "test:pingpong": "forge test -vvv --match-contract PingPongTest",
    "test:router": "forge test -vvv --match-contract PrivacyRouterTest",
    "phase2": "npm run probe-railgun && npm run verify-l1 && npm run test:pingpong && npm run test:router",
    "phase2:l2": "npm run check-l2",
    "deploy:sepolia": "bash scripts/deploy-l1-sepolia.sh",
    "test:all": "forge test -vvv"
  },
  "dependencies": {
    "snarkjs": "^0.5.0"
  },
  "devDependencies": {
    "ethers": "^6.16.0"
  }
}
